<style>
  /* ===== RESPONSIVE FIXES ===== */
@media (max-width: 992px) { /* md breakpoint */
  .search {
    margin-left: 0 !important; /* remove large left margin on smaller screens */
    margin-top: 0.5rem;
    width: 100%;
  }

  .search .form-control {
    width: 70%;
    margin-right: 0.5rem;
  }

  .navbar-nav.ms-auto.align-items-center.gap-2 {
    flex-direction: column;
    gap: 0.5rem;
    margin-top: 0.5rem;
  }

  .add-airbnb, .nav-btn, #darkModeToggle {
    width: 100%; /* full width buttons for small screens */
    text-align: center;
  }

  .navbar-nav .nav-link.explore {
    margin-bottom: 0.5rem; /* add spacing below explore */
  }
}

@media (max-width: 576px) { /* xs/sm screens */
  .search .form-control {
    width: 100%;
    margin-bottom: 0.5rem;
  }
  .search-btn {
    width: 100%;
  }
}

  /* ===== NAVBAR BASE STYLES ===== */
  .search {
    margin-left: 25rem !important;
  }

  .search-btn {
    background-color: #fe424d;
    color: white;
    border-radius: 25px;
    padding: 0 1rem;
  }
  .search-btn:hover {
    background-color: #fe424d;
    color: white;
  }
  .search-btn i {
    margin-right: 0.5rem;
  }
 
 
  .add-airbnb {
  display: inline-block; /* transform works */
  transition: transform 0.3s ease, opacity 0.3s ease;
  cursor: pointer;
  padding: 5px 10px;
  border-radius: 45px;
  opacity: 0.8;
 
  color: white;
  text-decoration: none; /* remove underline */
}

.add-airbnb:hover {
  transform: scale(1.1);
  opacity: 1;
}


.nav-btn {
  display: inline-block; /* allow transform to work */
 
  transition: transform 0.3s ease;
  cursor: pointer;
  padding:  5px  10px;
  
  opacity: 0.8;
}

.nav-btn:hover {
  transform: scale(1.1); /* 20% bigger on hover */
  opacity: 1;
}

.log{
  margin-left: -15px;
}


  .search-inp {
    border-radius: 25px;
    padding: 0.5rem 3rem;
    font-size: 1rem;
  }

  /* ===== DARK MODE STYLES ===== */
  body.dark-mode {
    background-color: #121212;
    color: #e4e4e4;
  }
  body.dark-mode .navbar {
    background-color: #1f1f1f !important;
  }
  body.dark-mode .nav-link {
    color: #e4e4e4 !important;
  }
  body.dark-mode .nav-btn {
    
    color: #e4e4e4;
  }
  body.dark-mode .search-inp {
    background-color: #333;
    color: white;
    border: 1px solid #555;
  }
.explore {
  transition: transform 0.3s ease; /* animate only transform */
}

/* Hover effect */
.explore:hover {
  transform: scale(1.2); /* 20% bigger on hover */
}

</style>

<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <!-- Brand -->
    <a class="navbar-brand" href="/listing"><i class="fa-solid fa-compass"></i></a>

    <!-- Toggler -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link explore" href="/listing">Explore</a>

        <!-- Search Bar -->
        <div class="navbar-nav ms-auto search">
  <form class="d-flex" action="/search" method="get" role="search">
    <input class="form-control me-2 search-inp" type="search" name="q" placeholder="Search destination" />
    <button class="btn search-btn d-flex align-items-center gap-2" type="submit">
      <i class="fa-solid fa-magnifying-glass"></i> Search
    </button>
  </form>
</div>

      </div>

      <!-- Right Side: Dark Mode + Auth -->
      <div class="navbar-nav ms-auto align-items-center gap-2">
        <!-- Dark Mode Toggle -->
        <button id="darkModeToggle" class="btn btn-outline-secondary rounded-pill px-3">🌙 Dark Mode</button>

        <a class="nav-link add-airbnb"  href="/listing/new">Airbnb your home</a>

        <% if (!currUser) { %>
          <a class="nav-link active" aria-current="page" href="/signup"><b class="nav-btn">SignUp</b></a>
          <a class="nav-link" href="/login"><b class="nav-btn log">Login</b></a>
        <% } %>

        <% if (currUser) { %>
          <a class="nav-link" href="/logout"><b class="nav-btn">Log out</b></a>
        <% } %>
      </div>
    </div>
  </div>
</nav>



<script>
  // ===== DARK MODE SCRIPT =====
  const toggleBtn = document.getElementById("darkModeToggle");
  const body = document.body;

  // Load preference from localStorage
  if (localStorage.getItem("darkMode") === "enabled") {
    body.classList.add("dark-mode");
    toggleBtn.textContent = "☀️ Light Mode";
  }

  toggleBtn.addEventListener("click", () => {
    body.classList.toggle("dark-mode");

    if (body.classList.contains("dark-mode")) {
      localStorage.setItem("darkMode", "enabled");
      toggleBtn.textContent = "☀️ Light Mode";
    } else {
      localStorage.setItem("darkMode", "disabled");
      toggleBtn.textContent = "🌙 Dark Mode";
    }
  });
</script>

